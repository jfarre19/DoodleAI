remote_theme: pages-themes/architect@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
defaults:
  - scope:
      path: "assets/img"
    values:
      image: true
sass:
  sass_dir: assets/

title: DoodleHI
description: Witness the marvelous drawings from a real live HI!